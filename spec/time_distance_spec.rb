require "spec_helper"

RSpec.describe TimeDistance do
  it "has a version number" do
    expect(TimeDistance::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
